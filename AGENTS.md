# Repository Agent Instructions

- Never use `git commit --no-verify` in this repository.
- Do not leave substantial implementations in a single large module. When a file starts mixing multiple responsibilities, split it into a small package or module set by responsibility, such as app assembly, rendering, styles, presentation/formatting, data access, or domain logic.
